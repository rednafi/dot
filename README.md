## DotFiles

* Restoring ssh key
   * Copy the backup ssh keys into `~/.ssh` folder
   * Change folder permission
        ```bash
        sudo chown -R $USER:$USER ~/.ssh
        chmod -R 700 ~/.ssh
        ```
